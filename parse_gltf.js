const fs = require('fs');
const gltfPipeline = require('gltf-pipeline');
const processGltf = gltfPipeline.processGltf;

// It's easier to just use three.js in node if we want to parse it, but I don't know if three is available.
